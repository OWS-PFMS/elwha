<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.owspfm</groupId>
    <artifactId>elwha-parent</artifactId>
    <version>1.1.2</version>
  </parent>

  <artifactId>elwha</artifactId>
  <packaging>jar</packaging>

  <name>Elwha</name>
  <description>A Swing component library implementing Material 3 Expressive on FlatLaf — a design-token foundation plus ~25 components, with the containers, overlays, and anchors around them.</description>
  <url>https://github.com/OWS-PFMS/elwha</url>

  <!-- The consumer-inherited compile scope: Swing + FlatLaf only (see CLAUDE.md's coupling
       stance and docs/consumer/stability.md). Test-scope deps come from the parent. -->
  <dependencies>
    <dependency>
      <groupId>com.formdev</groupId>
      <artifactId>flatlaf</artifactId>
      <version>${flatlaf.version}</version>
    </dependency>
    <dependency>
      <groupId>com.formdev</groupId>
      <artifactId>flatlaf-extras</artifactId>
      <version>${flatlaf.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <!-- Scoped to the main jar only (not plugin-level) so the test-jar below does not
                 inherit the module name — two jars claiming com.owspfm.elwha on a module path
                 would collide, and the test-jar is classpath-only test tooling (§3.2). -->
            <id>default-jar</id>
            <configuration>
              <archive>
                <manifestEntries>
                  <!-- #774: a stable JPMS name for module-path consumers; without it the module
                       name derives from the jar filename. -->
                  <Automatic-Module-Name>com.owspfm.elwha</Automatic-Module-Name>
                </manifestEntries>
              </archive>
            </configuration>
          </execution>
          <execution>
            <!-- §3.2 of docs/research/elwha-artifact-split-design.md [LOCKED]: the shared test
                 fixtures ship as a test-jar restricted to testkit/**, consumed test-scope by
                 elwha-showcase. The include keeps the ~200 per-component test classes off the
                 showcase test classpath. A third testkit module is rejected — test-scope edges
                 count in the reactor graph, so it is a module cycle. -->
            <id>testkit-test-jar</id>
            <goals><goal>test-jar</goal></goals>
            <configuration>
              <includes>
                <include>com/owspfm/elwha/testkit/**</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <configuration>
          <!-- #547: the landing page of the published javadoc jar (component index + quick
               start) instead of a bare package list. Module-specific — the doclint/tag config
               it merges with is inherited from the parent. -->
          <overview>${project.basedir}/src/main/javadoc/overview.html</overview>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
